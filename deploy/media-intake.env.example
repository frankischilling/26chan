MEDIA_INTAKE_MODE=development
MEDIA_INTAKE_BIND=127.0.0.1:3900
INTAKE_DATABASE_URL=postgres://board_media_intake:REPLACE@127.0.0.1:55432/imageboard
MEDIA_QUARANTINE_DIR=/var/lib/paperboard/quarantine
# Generate independent 32-byte random hex tokens. These placeholders are rejected.
MEDIA_INTAKE_TOKEN=REPLACE
METRICS_BIND_ADDR=127.0.0.1:9405
METRICS_TOKEN=REPLACE_WITH_DIFFERENT_TOKEN
